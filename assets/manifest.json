{
  "manifest_version": "0.3",
  "name": "assets-mcp",
  "display_name": "Assets",
  "version": "0.1.0",
  "description": "Search and fetch design assets: icons, illustrations, fonts, photos, textures, and 3D models, with license metadata.",
  "long_description": "An MCP server for design assets in design and development work — icons, illustrations, fonts, photos, textures, and 3D models, with license and attribution metadata throughout. The embedded icons/illustrations/fonts base and four keyless remote providers require no API keys; five further opt-in keyed providers register only when their API key or flag is configured, adding more photos and 3D models.\n\n**Key features:**\n\n- **Icons** — 8 vendored Iconify sets (Bootstrap Icons, Feather, Heroicons, Lucide, Material Symbols, Phosphor, Simple Icons, Tabler), rendered to standalone SVG with optional color and size overrides, plus the full keyless Iconify remote catalogue\n- **Illustrations** — 3 vendored CC0-1.0 SVG collections (Open Doodles, Humaaans, Open Peeps)\n- **Fonts** — 14 OFL-1.1 font families as woff2 files, with optional @font-face CSS generation, plus the keyless Google Fonts catalogue\n- **Photos** — keyless Openverse CC-licensed images, plus the opt-in Unsplash, Pixabay, and Pexels providers\n- **Textures** — keyless ambientCG CC0 PBR material archives\n- **3D models** — the opt-in Poly Pizza and Poly Haven providers\n- **License metadata** — every asset source carries its license and attribution, surfaced via `list_asset_sources`\n- **Offline-capable** — a vendored icons/illustrations/fonts base is always embedded at build time; set `ASSETS_DISABLE_REMOTE=1` to disable the four keyless remote providers and run with no network calls at runtime",
  "author": {
    "name": "John Beshir",
    "url": "https://github.com/jbeshir"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jbeshir/mcp-servers.git"
  },
  "license": "MIT",
  "server": {
    "type": "binary",
    "entry_point": "server/assets-mcp-linux-amd64",
    "mcp_config": {
      "command": "${__dirname}/server/assets-mcp-linux-amd64",
      "args": [],
      "env": {
        "ASSETS_OUTPUT_DIR": "${user_config.output_dir}",
        "ASSETS_DISABLE_REMOTE": "${user_config.disable_remote}",
        "ASSETS_CACHE_DIR": "${user_config.cache_dir}"
      },
      "platform_overrides": {
        "darwin": {
          "command": "${__dirname}/server/assets-mcp-darwin-arm64"
        },
        "win32": {
          "command": "${__dirname}/server/assets-mcp-windows-amd64.exe"
        }
      }
    }
  },
  "user_config": {
    "output_dir": {
      "type": "string",
      "title": "Output Directory",
      "description": "Directory to write rendered assets to (default: OS temp dir + /assets-mcp)",
      "sensitive": false,
      "required": false
    },
    "disable_remote": {
      "type": "string",
      "title": "Disable Remote Providers",
      "description": "Set to 1 to disable the keyless remote providers and run fully offline",
      "sensitive": false,
      "required": false
    },
    "cache_dir": {
      "type": "string",
      "title": "Cache Directory",
      "description": "On-disk cache dir for remote fetches (default: OS cache dir + /assets-mcp)",
      "sensitive": false,
      "required": false
    }
  },
  "tools": [
    { "name": "list_asset_sources", "description": "List asset providers and their sources with license and count; filter by kind, provider, or source" },
    { "name": "search_icons", "description": "Search icon sets (embedded + Iconify) by name; returns composite ids" },
    { "name": "get_icon", "description": "Render an icon to SVG by composite id and write it to disk" },
    { "name": "search_illustrations", "description": "Search vendored illustration collections by name; returns composite ids" },
    { "name": "get_illustration", "description": "Fetch an SVG illustration by composite id and write it to disk" },
    { "name": "search_fonts", "description": "Search font families (embedded + Google Fonts) by name, slug, or category; returns composite ids" },
    { "name": "get_font", "description": "Fetch a font's woff2 file (and optional @font-face CSS) by composite id and write it to disk" },
    { "name": "search_photos", "description": "Search keyless Openverse CC-licensed photos by name; returns composite ids" },
    { "name": "get_photo", "description": "Fetch a photo by composite id and write it to disk" },
    { "name": "search_textures", "description": "Search keyless CC0 ambientCG PBR material sets by name; returns composite ids" },
    { "name": "get_texture", "description": "Fetch a PBR material archive by composite id and write it to disk" },
    { "name": "search_models", "description": "Search 3D model providers (Poly Pizza, Poly Haven) by name; returns composite ids" },
    { "name": "get_model", "description": "Fetch a 3D model by composite id and write it to disk" }
  ],
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"]
  }
}
