startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({
      command: '/app/supermarkets-uk-mcp'
    })
build:
  dockerfile: Dockerfile
  dockerBuildPath: "."
